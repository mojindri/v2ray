# VM defaults for:
#   make vm-fingerprint-default
#   make local-total-with-vm
#
# Your machine has VirtualBox:
#   /usr/local/bin/VBoxManage
#
# IMPORTANT:
#   VM_NAME must match: VBoxManage list vms
#   VM_HOST must be the VM's real IP from inside VM: ip addr

VM_NAME=Ubuntu
VM_HOST=192.168.64.10
VM_USER=lab
VM_SSH_PORT=22
VM_TARGET_URL=https://www.cloudflare.com
VM_EXPECT_SNI=www.cloudflare.com
