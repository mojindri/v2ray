server {
    listen 443 ssl;
    server_name blackwire.local;
    ssl_certificate     /etc/nginx/certs/cert.pem;
    ssl_certificate_key /etc/nginx/certs/key.pem;
    location / {
        return 200 "tls-cover\n";
        add_header Content-Type text/plain;
    }
}
