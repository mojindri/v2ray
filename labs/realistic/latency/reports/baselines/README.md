# Latency Baselines

Sample reference results from the loopback smoke run.

**These are machine-specific.** Do not use these as universal pass/fail thresholds.
Each machine/environment must generate its own baselines with `make latency-local`.

See `docs/latency-lab.md` for the baseline policy and how to regenerate.
