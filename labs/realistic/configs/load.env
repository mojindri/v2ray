# Local load smoke defaults. The script skips instead of failing if SOCKS is not listening.
SOCKS_HOST=127.0.0.1
SOCKS_PORT=1080
TARGET_URL=http://127.0.0.1:18080/
CONCURRENCY=50
REQUESTS=250
CONNECT_TIMEOUT_SECS=3
READ_TIMEOUT_SECS=8
